[build]
  command   = "yarn workspace @leyfis/app build && cd packages/leyfis-mcp && npm install && npm run build"
  publish   = "apps/app/out"
  base      = "app"

[build.environment]
  NODE_VERSION = "20"
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
