[package]
name = "leyfis-gate"
version = "0.1.0"
description = "Leyfis Gate Program — institutional compliance infrastructure on Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "leyfis_gate"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
idl-build = ["anchor-lang/idl-build"]
default = []

[dependencies]
anchor-lang = "0.30.1"
